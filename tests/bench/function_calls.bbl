// function_calls.bbl — many function calls
// Measures: scope creation, callFn overhead
(def add (fn (a b) (+ a b)))
(def i 0)
(def sum 0)
(loop (< i 500000)
    (set sum (add sum i))
    (set i (+ i 1))
)
(print sum)
